* {
margin: 0;
padding: 0;
box-sizing: border-box;
font-family: 'Segoe UI', sans-serif;
}

/* Background */
body {
    min-height: 100vh;
    background: url("santa2.jpg") no-repeat center center/cover;
    overflow: hidden;
    color: white;
}

html, body {
    width: 100%;
    height: 100%;
    margin: 0;
    overflow: hidden;          /* 🚫 no scroll */
    overscroll-behavior: none; /* 🚫 bounce */
    touch-action: none;        /* 🚫 pinch zoom */
}



/* Overlay */
.overlay {
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.45);
    z-index: -1;
}

/* Glass UI */
.glass {
    background: rgba(255,255,255,0.15);
    backdrop-filter: blur(5px);
    border-radius: 25px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.4);
}

/* Lock Screen */
.lock {
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    padding: 30px;
}

.lock h1 {
    font-size: 36px;
    margin-bottom: 10px;
}

.pin-text {
    font-size: 16px;
    margin-bottom: 20px;
    opacity: 0.9;
}

/* PIN Boxes */
.pin-box {
    display: flex;
    gap: 10px;
    margin-bottom: 15px;
}

.pin-input {
    width: 45px;
    height: 55px;
    font-size: 18px;   /* MUST be >= 16px */
    text-align: center;
    border-radius: 12px;
    border: none;
    outline: none;
    background: rgba(255,255,255,0.9);
    touch-action: manipulation;
}


#error {
margin-top: 10px;
color: #ffd54f;
}

/* Main Content */
.hidden {
    display: none;
}

.container {
    width: 90%;
    max-width: 95%;
    margin: auto;
    margin-top: 50px;
    padding: 25px;
    text-align: center;
}

#typing {
max-width: 90%;
margin: 0 auto 20px auto;   /* center with proper margin */
margin-top:20px;
min-height: 40px;

font-size: 30px;
line-height: 1.2;          /* better readability */
text-align: center;

color: #ffe4ec;
padding: 10px 15px;

white-space: normal;       /* allow wrapping */

white-space: pre-wrap;  /* 🔥 THIS FIXES SPACES */
overflow-wrap: break-word;

font-family: 'Old English Text MT', 'Cinzel', 'Georgia', serif;
}


/*#typing {
max-width: 90%;
margin: 20px auto;

font-size: 28px;
line-height: 1.6;
text-align: center;

color: #ffe4ec;
padding: 18px 20px;

word-wrap: break-word;

}*/



/* Scratch Card */
.card {
    width: 280px;
    height: 150px;
    margin: auto;
    margin-top:30px;
    position: relative;
    border-radius: 20px;
    overflow: hidden;
    background: white;
}

.message {
    position: absolute;
    inset: 0;
    color: #e91e63;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    font-size: 22px;
    font-weight: bold;
    padding: 10px;
}

canvas {
    position: absolute;
    inset: 0;
}

.note {
    margin-top: 12px;
    font-size: 14px;
    opacity: 0.9;
}

canvas {
    touch-action: none;
}



.site-footer {
    position: fixed;
    bottom: 10px;
    width: 100%;
    text-align: center;

    font-size: 14px;
    letter-spacing: 0.5px;

    color: rgba(255, 255, 255, 0.75);
    font-family: 'Poppins', 'Segoe UI', sans-serif;

    z-index: 999;
    pointer-events: none; /* footer doesn't interfere with touch */
}

.site-footer strong {
    color: #ffd6e5;
    text-shadow: 0 0 6px rgba(255, 182, 193, 0.6);
}



.name-image {
    display: flex;
    justify-content: center;
    align-items: center;

    margin: 20px;

    z-index: 5;
}

.name-image img {
    max-width: 60%;
    height: auto;

    border-radius: 12px;

    /* soft glow */
    box-shadow: 0 0 20px rgba(255, 105, 180, 0.6);

    /* responsive scaling */
    object-fit: contain;
}

.name-image img {
    animation: fadeIn 1.5s ease-in-out;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(-10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes pulse {
    0% { box-shadow: 0 0 10px rgba(255,105,180,0.4); }
    50% { box-shadow: 0 0 25px rgba(255,105,180,0.9); }
    100% { box-shadow: 0 0 10px rgba(255,105,180,0.4); }
}

.name-image img {
    animation: pulse 3s infinite;
}


.hoho_img{width:100%;height:200px;}
